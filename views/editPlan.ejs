<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Calendar App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Calendar App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/all">Show all</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/addPlan">Add new plan</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update plan</h2></div>
        <br>
        <div class="text-center">
          <% if (plan) { %> 
            <form action="/editPlan/<%= plan.planId %>" method="POST" enctype="multipart/form-data">
                <label for="plan">Plan:</label><br>
                <input type="text" id="plan" name="plan" value = <%= plan.plan %> required><br><br>

                <label for="date" >Date:</label><br>
                <input type="date" id="date" name="date" min="0" step="1" value = <%= plan.date %> required><br><br>

                <label for="time">Time:</label><br>
                <input type="text" id="time" name="time" min="0" step="0.01" value = <%= plan.time %> ><br><br>

                <label for="image">Current Image:</label><br>
                <input type="text" name="currentImage" value="<%= plan.image %>" disabled><br>
                <img src="/images/<%= plan.image %>" ><br><br>

                <label for="image">Image:</label><br>
                <input type="file" id="image" name="image" accept="image/*" value=<%=plan.image%>><br><br>

                
                <button type="submit">Update plan</button>
            </form>
          <% } else { %>
              <p>No plan found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>